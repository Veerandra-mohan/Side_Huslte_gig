services:
  # Backend Service (Node.js)
  - type: web
    name: side-hustle-backend
    env: node
    rootDir: backend
    plan: free # or your desired plan
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGO_URI
        fromDatabase:
          name: side-hustle-db # Make sure this matches the name of your Render Database
          property: connectionString
      - key: JWT_SECRET
        generateValue: true # Let Render generate a secure secret
      - key: FRONTEND_URL
        value: "https://side-hustle-frontend.onrender.com" # Your frontend URL for CORS
      - key: PORT
        value: 5000

  # Frontend Service (React Static Site)
  - type: static_site
    name: side-hustle-frontend
    rootDir: frontend
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: side-hustle-backend
          property: url